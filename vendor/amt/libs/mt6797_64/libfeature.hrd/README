Library of MTK camera heart rate detection.

This library is pre-built by MediaTek internal.


WHAT IT DOES?
=============
This contains MediaTek developed camera heart rate detectiont module.  
Without them, camera heart rate detection will not work”

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:
1.  liblog.so
2.  libcutils.so

and the following libs from MediaTek:
1.  libcameracustom.so
2.  libdpframework.so

It can build the following static library file :
libcam.featureio.pipe.hrd.a

HOW TO USE IT?
==============

Provide api for camera HAL to control camera heart rate detection feature.
all api interface are at (vendor/mediatek/proprietary/hardware/mtkcam)

All the source code of this library were written by MediaTek co..
